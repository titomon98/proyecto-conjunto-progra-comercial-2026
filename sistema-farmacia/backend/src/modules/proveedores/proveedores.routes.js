// Rutas del modulo proveedores.
// Responsabilidad: mapear cada endpoint HTTP con su funcion del controller.
const { Router } = require('express');

const router = Router();

// TODO (equipo proveedores): declarar aqui las rutas del modulo.
// Ejemplo: router.get('/', proveedoresController.listar);

module.exports = router;
